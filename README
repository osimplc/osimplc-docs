# Doc site for OSIMPLC

Based on https://github.com/docpress/docpress

## Pre requisites

Install NodeJS and npm

## How to use:

git clone (this repo)

npm install 

npm run build:es
(generates static site at dist/es)

npm run build:en
(generates static site at dist/en)

Then cd into either dist/es or dist/en and launch a web server from ther, e.g.

python -m SimpleHTTPServer

Then browse to http://localhost:8000

