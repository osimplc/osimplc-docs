{
  "name": "osimplc-docs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run start:es",
    "start:es": "ln -s es docs && ln -s README.es.md README.md && docpress s",
    "build": "npm run build:es",
    "build:es": "rm docs README.md && ln -s es docs && ln -s README.es.md README.md && docpress build && mv _docpress _docs_es",
    "build:en": "rm docs README.md && ln -s en docs && ln -s README.en.md README.md && docpress build && mv _docpress _docs_en"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "docpress": "0.7.6",
    "markdown-it-decorate": "^1.2.2"
  },
  "dependencies": {
    "docpress-base": "^0.7.6",
    "docpress-core": "^0.9.0",
    "docpress-simple-search": "^0.6.1",
    "markdown-it-link-attributes": "^2.1.0"
  }
}
